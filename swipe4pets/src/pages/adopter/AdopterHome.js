import React from "react";

const AdopterHome = () => {
  return (
    <h1>
      This is the adopter user home page or matches page. Here a user can view
      animals they have already matched with, manage those matches, or start a
      new pet search.
    </h1>
  );
};

export default AdopterHome;
