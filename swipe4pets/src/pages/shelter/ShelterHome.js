import React from "react";

const ShelterHome = () => {
    return (
        <div>
            <h1>
                This is the shelter home page. This is the first page a user sees after logging in as a shelter user. Where a user can view, create and manage shelter pet profiles.
            </h1>
        </div>
    );
};

export default ShelterHome;